/**
 * @deprecated
 * 이 컴포넌트는 RESTORE R1에 의해 폐기되었습니다.
 * 모든 최신 작업 공간 로직은 'workspace/WorkspaceRoot.tsx'에 구현되어 있습니다.
 * 거버넌스(StageGate, Compliance, Audit 등) 로직은 사용자 요구에 따라 제거되었습니다.
 */
import React from 'react';

const DeprecatedWorkspaceRoot: React.FC = () => {
  return null;
};

export default DeprecatedWorkspaceRoot;
